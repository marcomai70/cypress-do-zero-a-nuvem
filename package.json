{
  "name": "cypress-do-zero-a-nuvem",
  "version": "1.0.0",
  "description": "Curso Cypress do Zero à Nuvem",
  "scripts": {
    "test": "cypress run",
    "cy:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^13.12.0",
    "http-server": "^14.1.1",
    "start-server-and-test": "^2.1.2"
  },
  "dependencies": {
    "@cypress/puppeteer": "^0.1.8"
  }
}
